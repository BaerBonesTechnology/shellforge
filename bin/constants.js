import os from 'os';

export const PACKAGE_NAME = 'shellforge';

export const ConfigKey = Object.freeze({
  announcements: 'announcements',
  announcementShown: 'announcementShown',
  defaultScriptPath: 'defaultScriptPath',
  defaultTextEditorCommand: 'defaultTextEditorCommand',
  defaultTextEditorPath: 'defaultTextEditorPath',
  initMessages: 'init_messages',
  initialized: 'initialized',
  scriptCommandDir: 'scriptCommandDir',
  scriptDir: 'scriptDir',
  scriptFileName: 'scriptFileName',
  terminalProfile: 'terminalProfile',
  tutorial: 'tutorial',
});

export const ScriptKey = Object.freeze({
  name: 'name',
  path: 'path',
  script: 'script',
});

export const ContentKey = Object.freeze({
  finished: 'finished',
  message: 'message',
  messages: 'messages',
  output: 'output',
  steps: 'steps',
  subSteps: 'subSteps',
  title: 'title',
  version: 'version',
});

export const Shell = Object.freeze({
  bash: 'bash',
  zsh: 'zsh',
  powershell: 'powershell',
  cmd: 'cmd',
  get all() { return [this.bash, this.zsh, this.powershell, this.cmd]; },
  get platformDefault() { return os.platform() === 'win32' ? this.powershell : this.bash; },
});

export const ShellExt = Object.freeze({
  sh: '.sh',
  ps1: '.ps1',
  bat: '.bat',
});

export const ShellRunner = Object.freeze({
  sh: 'sh',
  powershell: 'powershell',
  cmd: 'cmd',
});

export const ShellRunnerArgs = Object.freeze({
  powershellFile: '-File',
  cmdExec: '/c',
});

export const EnvVar = Object.freeze({
  home: '$HOME',
  userProfile: '%USERPROFILE%',
});

export const Defaults = Object.freeze({
  editor: 'code',
  fallbackEditorWindows: 'notepad',
  fallbackEditorUnix: 'open',
  scriptDirName: '.scripts',
  commandsDirName: 'commands',
  configDirName: '.shellforge',
  configFile: 'config.json',
  scriptsFile: 'scripts.json',
  scriptPath: '.',
  tempRunPrefix: '_temp_run',
  scriptBaseName: 'script',
  userHomePlaceholder: '$USER_HOME',
});

export const AnnouncementFilter = Object.freeze({
  list: 'LIST',
  all: 'ALL',
});

export const ReinitOption = Object.freeze({
  move: 'Move To New Location',
  delete: 'Delete Existing Scripts',
  cancel: 'Cancel',
  get all() { return [this.move, this.delete, this.cancel]; },
});

export const ParamType = Object.freeze({
  required: 'required',
  optional: 'optional',
  nullable: 'nullable',
});

export const ScriptDisclosure = Object.freeze({
  bash: (version) => `# This script is generated by ${PACKAGE_NAME} v:${version}\n\n`,
  bat: (version) => `REM This script is generated by ${PACKAGE_NAME} v:${version} \n\n####`,
  ps1: (version) => `<#\n This script is generated by ${PACKAGE_NAME} v:${version}\n #>`,
});
